/**
 * FREEZE_STATS — cuando está en true, todos los dashboards y KPIs muestran
 * valores en 0 / listas vacías, sin consultar datos reales.
 * Cambiar a false para volver a mostrar datos operativos reales.
 */
export const FREEZE_STATS = true
